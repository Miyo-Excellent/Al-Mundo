//-- Initial State --//
const initialState = {};

//-- Action --//
//  import {} from '../actions/hotels';

//-- Action Types --//
//  import {} from '../actionTypes';

//-- Reducer --//
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
